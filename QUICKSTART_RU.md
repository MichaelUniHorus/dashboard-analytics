# Быстрый старт 🚀

## Минимальные шаги для запуска

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить пакеты
pip install -r requirements.txt
```

### 2. Создать демо-данные

```bash
python scripts/seed_data.py
```

Это создаст:
- ✅ База данных SQLite (`dashboard.db`)
- ✅ 200 финансовых транзакций
- ✅ 500 метрик оборудования

### 3. Запустить сервер

```bash
python -m uvicorn app.main:app --reload
```

Или просто:

```bash
python run.py
```

### 4. Открыть в браузере

**Главная страница:**
- Английский: http://localhost:8000/?lang=en
- Русский: http://localhost:8000/?lang=ru

**Дашборды:**
- Транзакции: http://localhost:8000/transactions?lang=ru
- Оборудование: http://localhost:8000/equipment?lang=ru

**API документация:**
- Swagger: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Переключение языка

### Способ 1: Через URL
Добавьте `?lang=ru` или `?lang=en` к любому URL

### Способ 2: Через интерфейс
В правом верхнем углу навигации есть выпадающее меню 🌐

## Структура проекта

```
dashboard-project/
├── app/
│   ├── main.py              # Точка входа FastAPI
│   ├── i18n.py              # Переводы (en/ru)
│   ├── models/              # Модели БД
│   ├── services/            # Бизнес-логика
│   ├── routers/             # API эндпоинты
│   └── templates/           # HTML шаблоны
├── scripts/
│   └── seed_data.py         # Генерация демо-данных
└── requirements.txt         # Зависимости
```

## Основные файлы

| Файл | Описание |
|------|----------|
| `app/main.py` | Главный файл приложения |
| `app/i18n.py` | Словари переводов |
| `app/models/transaction.py` | Модель финансовых транзакций |
| `app/models/equipment_metric.py` | Модель метрик оборудования |
| `scripts/seed_data.py` | Скрипт создания демо-данных |

## Полезные команды

```bash
# Запуск с автоперезагрузкой
python -m uvicorn app.main:app --reload

# Запуск на другом порту
python -m uvicorn app.main:app --port 8080

# Пересоздать демо-данные
python scripts/seed_data.py
```

## Что дальше?

- 📖 Читайте [README_RU.md](README_RU.md) для полной документации
- 🌍 Изучите [I18N_GUIDE.md](I18N_GUIDE.md) для работы с переводами
- 🧪 Следуйте [TESTING_I18N.md](TESTING_I18N.md) для тестирования

## Возможные проблемы

### Порт 8000 занят
```bash
# Используйте другой порт
python -m uvicorn app.main:app --port 8001
```

### База данных не создаётся
```bash
# Сначала запустите скрипт создания данных
python scripts/seed_data.py
```

### Ошибка импорта модулей
```bash
# Убедитесь, что venv активирован и зависимости установлены
pip install -r requirements.txt
```

## Готово! 🎉

Теперь у вас работает полнофункциональная аналитическая панель с поддержкой двух языков!
